Run the frontend docker image -> docker build -t restuarant-frontend .
Run the backend docker image -> docker build -t restuarant-backend .


Yml file build and run

docker-compose build
docker-compose up
